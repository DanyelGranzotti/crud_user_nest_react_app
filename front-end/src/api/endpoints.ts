/**
 * Endpoints da API utilizados no projeto.
 */
export const API_ENDPOINTS = {
  USERS: "/users",
  COLORS: "/colors",
  NOTES: "/notes",
  AUTH: {
    LOGIN: "/auth/login",
    REFRESH: "/auth/refresh",
  },
};
